<!--
  Listed in the panel catalog (src/data/mock.js) so it's a real, openable
  dock tab, but the design canvas never specced what a saved filter list
  looks like — say so rather than guessing at a design that wasn't given.
-->
<template>
  <div class="panel-fill">
    <div class="insp-empty">Saved filters hasn't been designed yet — nothing in the design canvas covers this panel.</div>
  </div>
</template>
